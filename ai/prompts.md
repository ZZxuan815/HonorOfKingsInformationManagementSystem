# AI Prompts Log

| # | Time | Tool/Model | Agent Role | Prompt Text | Response Summary | Human Decision | Git Commit |
|---|------|-----------|------------|-------------|------------------|----------------|------------|
| 1 | Stage 2 | GPT-4o (via opencode) | AI-Architect | "Design the Person class hierarchy with Player and Admin subclasses. Include serialVersionUID, constructors, and fields matching the spec requirements for id, name, password, and role." | Generated abstract `Person` class with `Player` and `Admin` extending it. Provided full field lists, getters/setters, and `toString()` overrides. Suggested `Serializable` on Person, `HashMap<String, Player>` for fast ID lookup in the data manager. | Accepted. Manually reviewed hierarchy; adjusted `Person` to be abstract. | `5f1ad2b` |
| 2 | Stage 3 | GPT-4o (via opencode) | AI-Architect | "Give me a simple logic layout for the main menu loop - show a basic login menu first, then display different options for Player (read-only) vs Admin (full control). Just text flow, no Java code." | Produced a complete console text-flow: Login Screen → Role Dispatch → Player Menu (5 options) or Admin Menu (Hero/Equipment sub-menus). Included flow diagram with logout → login loop. | Accepted and used as blueprint for the entire menu structure. | `648126f` |
| 3 | Stage 5 | GPT-4o (via opencode) | AI-Implementation | "Implement two ranking methods: Player Leaderboard sorted by (winRate*100)+level descending with alphabetical tiebreaker, and Equipment Statistics sorted by usageCount*winRateContribution descending with alphabetical tiebreaker." | Generated `RankingService` with `Collections.sort()` using custom `Comparator<Player>` and `Comparator<Equipment>`. Both sort descending by computed score, ascending by name as tiebreaker. Defensively copies lists from data manager. | Accepted. Verified tie-breaking with edge cases. | `012a972` |
| 4 | Stage 6 | GPT-4o (via opencode) | AI-Implementation | "Implement FileStorageService to save/load GameDataManager via Java serialization to data.ser. Handle FileNotFoundException, IOException, ClassNotFoundException gracefully." | Generated `FileStorageService` with try-with-resources `ObjectOutputStream`/`ObjectInputStream`. Load method returns `null` if file missing (falls back to DataInitializer). Save prints success/error messages. | Accepted. Integrated into app startup (load) and every admin CRUD operation (auto-save). | `63166f3` |
| 5 | Stage 6 | GPT-4o (via opencode) | AI-Implementation | "Implement Searchable.matches(String keyword) on Player, Hero, Equipment, Team, and MatchRecord. Each checks relevant fields case-insensitively. Then add a search() method to SearchService that iterates all 5 collections." | Generated `matches()` for each model class checking 3-4 fields per class (e.g. Player checks id, name, ownedHeroes). `SearchService.search()` collects all matching items into a flat `List<Searchable>`. | Accepted. Manually verified edge cases (null ownedHeroes, empty lists). | `63166f3` |
| 6 | Stage 4 | GPT-4o (via opencode) | AI-Implementation | "Write AuthenticationService.java with private Person currentUser, login(id,password) checking admins first then players, logout(), isAdmin() using instanceof, and getCurrentUser()." | Generated `AuthenticationService` with `GameDataManager` injection, sequential admin→player lookup, password.equals() validation, and `instanceof Admin` check for `isAdmin()`. | Accepted. Added import for Player even though only Admin check is needed — kept for clarity. | `b51e15e` |
| 7 | Stage 6-7 | GPT-4o (via opencode) | AI-Implementation | "Add 5 more players (P011 through P015) to DataInitializer.java so that all 3 teams have at least 5 members each. Redistribute the 15 players evenly: T01 gets 5, T02 gets 5, T03 gets 5." | Generated 5 new Player objects with unique names, passwords, levels (20-50), win rates (0.4-0.8), and 3 owned heroes each. Updated initTeams() to reflect 5 members per team and adjusted totalMatches/wins. | Accepted. Verified all 15 players count and team distribution. | `dcf442e` |
| 8 | Stage 6-7 | GPT-4o (via opencode) | AI-Implementation | "In DataInitializer.java, populate equippedItems for each existing player (P001-P015). For each player's 3 owned heroes, assign 2-3 equipment items from the existing pool." | Generated `getEquippedItems().put()` calls for all 45 hero-to-equipment mappings across 15 players. Each hero gets 2-3 items from the existing 22-equipment pool. | Accepted. Verified equipment names match existing pool entries. | `4112153` |
| 9 | Stage 6-7 | GPT-4o (via opencode) | AI-Architect | "I have a MatchResult enum (WIN, LOSS, DRAW) that is never used. Should I delete it or use it? If using it is simple, add a MatchResult result field to MatchRecord." | Recommended adding `MatchResult result` field to `MatchRecord` with getter/setter, and a `determineResult()` helper in `DataInitializer` that compares winner vs teamA/teamB to set WIN/LOSS/DRAW for all 11 match records. | Accepted. Keeping the enum is cleaner than deleting it. | `4112153` |
| 10 | Stage 6-7 | GPT-4o (via opencode) | AI-Implementation | "In the match history view method, after listing individual match records for a Player or Team, append an aggregate statistics section with win/loss record and hero pick rates." | Generated aggregate stats loop counting wins/losses from the filtered results, plus hero pick rate calculation using a `Map<String, Integer>` count divided by total picks, printed as formatted percentages. | Accepted. Added alphabetical sorting for hero names in pick rate display. | `79a5e85` |
| 11 | Stage 6-7 | GPT-4o (via opencode) | AI-Implementation | "Expand the Leaderboard menu to allow the user to select sorting preference: [1] Custom Score [2] Win Rate [3] Level [4] Number of Matches." | Added `getPlayerRanking(int sortMode)` with 4 Comparator branches, plus `countMatchesForPlayer()` helper. Updated `viewPlayerLeaderboard()` to show a sorting submenu and dynamic column header. | Accepted. Alphabetical tie-breaking preserved across all 4 modes. | `79a5e85` |
| 12 | Stage 6-7 | GPT-4o (via opencode) | AI-Implementation | "Ensure that when a Hero, Player, or Team is deleted via GameDataManager, cascading deletions execute to maintain data consistency." | Generated cascade logic: `removeHero` removes hero name from all players' ownedHeroes and equippedItems; `removePlayer` removes player ID from team's memberIds; `removeTeam` resets teamId for all members to empty string. | Accepted. Manually verified edge cases (player not in any team, hero owned by no one). | `79a5e85` |
